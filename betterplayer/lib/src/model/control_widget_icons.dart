import 'package:flutter/cupertino.dart';

class ControlIcons {
  Widget? skipBackIcon;
  Widget? skipForwardIcon;
  Widget? playIcon;
  Widget? pauseIcon;
  Widget? fullscreenDisableIcon;
  Widget? fullscreenEnableIcon;
  Widget? muteIcon;
  Widget? unMuteIcon;

  ControlIcons(
  {this.skipBackIcon,
    this.skipForwardIcon,
    this.playIcon,
    this.pauseIcon,
    this.fullscreenDisableIcon,
    this.fullscreenEnableIcon,
    this.muteIcon,
    this.unMuteIcon});
}
